Console.Write("Введите число: ");
int  n = Convert.ToInt32(Console.ReadLine());
while (n >  0 && n < 10000)
    Console.Write("Введите число: ");
    if (n = n % 10);
   
else
{
    Console.WriteLine("нет");
}